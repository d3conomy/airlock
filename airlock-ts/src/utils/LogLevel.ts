/**
 * Log Level
 * @category Logging
 * @description Log levels - determine the severity of the log entry
 */
enum LogLevel {
    Debug = 0,
    Info = 1,
    Warning = 2,
    Error = 3,
    Fatal = 4
}

export {
    LogLevel
}